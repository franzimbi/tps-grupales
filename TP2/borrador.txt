#include "usuarios.h"
#include "hash.h"
#include "heap.h"
#include <stdio.h>
#include <stdlib.h>



// LOGIN Y LOGOUT

typedef struct{
    void** datos;
    size_t tamano;
    size_t capacidad;
}vector_t;

typedef struct{
    vector_t* vector_usr; (vector de usuario_t*)
    hash_t* hash_usr;  (hash de usuario_t*)
    vector_t* vector_likes; (vector de abb_t*);
    vector_t* vector_posts;
}global_t;

typedef struct{
    char* nombre;
    size_t id;
    heap_t* feed;
}usuario_t;

typedef struct{
    size_t id;
    char* mensaje;
    size_t id_creador;
}publicacion_t;

typedef struct{
    size_t id_publicacion;
    size_t prioridad;
}post_con_prioridad_t;

int cmp_publicacion(post_con_prioridad_t* a, post_con_prioridad_t* b){
    return a->prioridad - b->prioridad;
}